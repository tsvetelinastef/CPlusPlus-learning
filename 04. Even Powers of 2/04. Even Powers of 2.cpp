#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int power;
    cin >> power; 

    for (int i = 1; i <= (pow(i, power)) / 2;  i++)
    {
        cout << i << endl;
    }
    
    return 0;
}
